services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: supportsmith
      POSTGRES_USER: supportsmith
      POSTGRES_PASSWORD: supportsmith
    ports:
      - "55432:5432"
    volumes:
      - supportsmith-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U supportsmith -d supportsmith"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    env_file:
      - .env
    environment:
      SUPPORTSMITH_ENVIRONMENT: local
      DATABASE_URL: postgresql://supportsmith:supportsmith@postgres:5432/supportsmith
      # Phase 7: when SUPPORTSMITH_API_BEARER_TOKEN is set in .env, every
      # non-/health route requires `Authorization: Bearer <token>`. Admin
      # ingestion routes additionally require SUPPORTSMITH_ADMIN_API_KEY.
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      seed:
        condition: service_completed_successfully

  migrate:
    build: .
    command: ["uv", "run", "supportsmith-migrate"]
    environment:
      SUPPORTSMITH_ENVIRONMENT: local
      DATABASE_URL: postgresql://supportsmith:supportsmith@postgres:5432/supportsmith
    depends_on:
      postgres:
        condition: service_healthy

  seed:
    build: .
    command: ["uv", "run", "supportsmith-seed"]
    env_file:
      - .env
    environment:
      SUPPORTSMITH_ENVIRONMENT: local
      DATABASE_URL: postgresql://supportsmith:supportsmith@postgres:5432/supportsmith
    depends_on:
      migrate:
        condition: service_completed_successfully

volumes:
  supportsmith-postgres-data:
